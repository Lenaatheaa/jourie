package com.example.jourie.presentation.profile.components

import androidx.compose.runtime.Composable

// Deprecated: avatar header moved into `UserDetailsCard`
@Deprecated("Replaced by UserDetailsCard which now contains avatar and name")
@Composable
fun UserAvatarHeader(name: String) {
    // Removed - kept for backward compatibility. Do not use.
}
